import { EditableTextDirective } from './editable-text.directive';

describe('EditableTextDirective', () => {
  it('should create an instance', () => {
    const directive = new EditableTextDirective();
    expect(directive).toBeTruthy();
  });
});
